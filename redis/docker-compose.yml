version: '3'

services:
  redis:
    image: redis:7
    container_name: branch-redis
    ports:
      - "6380:6379"
    command: >
      redis-server
      --requirepass redisPass123
      --user default